pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls -R'
            }
        }
        stage('Test') {
            steps {
                sh 'ls'
            }
        }
    }
}
